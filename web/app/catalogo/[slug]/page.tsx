import Link from "next/link";
import viveCatalog from "../../../data/vive_catalog.json";
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=viveCatalog.find(item=>item.slug===slug);if(!product)return <article className="card"><h1>Producto no encontrado</h1><p><Link href="/catalogo">← Volver al catálogo</Link></p></article>;return <article className="card product-detail"><div className="detail-image"><img src={product.image_urls[0]} alt={product.name}/></div><p className="muted">{product.category}</p><h1>{product.name}</h1><p className="product-description detail-description">{product.description}</p><p className="muted">Precio por confirmar</p><button type="button">Agregar al carrito</button><p><Link href="/catalogo">← Volver al catálogo</Link></p></article>;}
